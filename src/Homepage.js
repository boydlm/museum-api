/*import React from 'react';
import MuseumSelect from './components/MusuemSelect.js'

export default function HomePage({ onSelectExhibition }) {
    const [selectedExhibition, setSelectedExhibition] = React.useState('');
  
    const handleChange = (event) => {
      const exhibitionId = event.target.value;
      setSelectedExhibition(exhibitionId);
      onSelectExhibition(exhibitionId); // Call the callback function passed from App.js
    };

  return (
    <div>
        <MuseumSelect/>
    </div>
  );
}*/

